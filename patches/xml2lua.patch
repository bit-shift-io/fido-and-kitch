diff --git a/lib/xml2lua/xml2lua.lua b/lib/xml2lua/xml2lua.lua
index 813cf1e..fe91be5 100755
--- a/lib/xml2lua/xml2lua.lua
+++ b/lib/xml2lua/xml2lua.lua
@@ -50,7 +50,12 @@
 --@author Paul Chakravarti (paulc@passtheaardvark.com)
 --@author Manoel Campos da Silva Filho
 local xml2lua = { _VERSION = "1.6-1" }
-local XmlParser = require("XmlParser")
+-- Upstream requires "XmlParser" as a top-level module; this project vendors
+-- xml2lua under lib/xml2lua/ and requires it as "lib.xml2lua.xml2lua" (see
+-- lib/sti/init.lua for the same cwd-relative require convention), so the
+-- require here is rewritten to resolve relative to this file's own path.
+local cwd = (...):gsub('%.xml2lua$', '') .. "."
+local XmlParser = require(cwd .. "XmlParser")
 
 ---Recursivelly prints a table in an easy-to-ready format
 --@param tb The table to be printed
